# Optional: full base URL of the game API (no trailing slash). In `vite` dev, REST
# calls default to http://127.0.0.1:3847 so they work even without the Vite /api proxy.
# VITE_SOCKET_URL=https://your-api.example.com

VITE_STELLAR_NETWORK=testnet
